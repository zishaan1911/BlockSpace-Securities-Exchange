# Copy to .env and fill in real values. Never commit .env or real keys
# (GLOSSARY.md's Golden Rules). All variables are GASX_AI_-prefixed and
# read by config.py via pydantic-settings.

# Ethereum ingestion (ingestion/ethereum.py). Any JSON-RPC endpoint with
# eth_getBlockByNumber/eth_feeHistory support; txpool_status is used if
# available but not required (see ingestion/ethereum.py).
GASX_AI_ETHEREUM_RPC_URL=https://ethereum-rpc.publicnode.com

# Sui oracle publish (oracle/publisher.py; ARCHITECTURE.md §6). Leave
# blank to disable publishing entirely — POST /publish returns 501 until
# all three of these are set.
GASX_AI_SUI_RPC_URL=https://fullnode.testnet.sui.io:443
# Sui base64 or bech32 keystring for the address gasx::oracle::set_publisher
# authorized as this market's oracle publisher. This should be a
# throwaway/dedicated key for this one job, funded with only what it
# needs for gas — never a wallet holding real user funds.
GASX_AI_SUI_PUBLISHER_PRIVATE_KEY=
# Deployed contracts/gasx package ID (from `sui client publish`).
GASX_AI_SUI_PACKAGE_ID=
# The OracleState shared object ID for the EGSI-1H market (from
# gasx::oracle::create_oracle's return value / the resulting object on
# Sui explorer).
GASX_AI_SUI_ORACLE_OBJECT_ID=

# EGSI history buffer size (features/history.py).
GASX_AI_EGSI_HISTORY_MAX_LEN=200
