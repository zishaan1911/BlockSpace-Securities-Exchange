{
  "name": "@gasx/engine",
  "version": "0.1.0",
  "private": true,
  "description": "N-API binding for the C++ order engine (engine/protocol/EngineService). Gives the gateway a fast off-chain order book and quote layer.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "install": "node build.js",
    "build": "node build.js",
    "test": "node test.js"
  },
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "node-gyp": "^11.0.0"
  },
  "gypfile": true
}
